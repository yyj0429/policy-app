import { View } from 'react-native';
import Typography from '../components/Typography';

export default function CardList() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Typography>CardList.tsx</Typography>
    </View>
  );
}
